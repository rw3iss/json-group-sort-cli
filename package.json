{
    "name": "json-group-sort-cli",
    "version": "1.0.0",
    "description": "Takes any input json file, groups duplicate entries by key prefix, ie \"key\" or \"key.something\" and breaks them apart, groups them into the \"key\", then child properties the same, and then it sorts it all. A config file can be used to specify group and sort options.",
    "author": "Ryan Weiss (rw3iss@gmail.com)",
    "license": "ISC",
    "main": "index.js",
    "type": "commonjs",
    "bin": {
        "json-group-sort": "index.js"
    },
    "scripts": {
        "test": "rm -rf test/out && node index.js -i=test/settings.json -o=test/out"
    },
    "dependencies": {
        "caporal": "^1.4.0",
        "comment-json": "^4.2.3"
    }
}